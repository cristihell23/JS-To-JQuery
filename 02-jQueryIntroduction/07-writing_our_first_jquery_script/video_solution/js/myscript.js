$(document).ready(function() {
    $('h2').addClass('underline') // will change the text-decoration to underline. Can use .removeClass
    $('ul').addClass('border'); // add border to all ul. Can use .removeClass
});